"""Method B placeholder — full-cell curve fitting (Phase 1 stub)."""

from __future__ import annotations

from typing import Any


def fit_degradation_parameters(*_args: Any, **_kwargs: Any) -> dict[str, Any]:
    """Reserved for synthetic LLI/LAM curve fitting.

    Returns empty result until a validated parametric model is registered.
    """
    raise NotImplementedError(
        "Method B curve fitting is reserved for a validated model; "
        "use Method A pattern scores (diagnose_feature_table) for now."
    )
