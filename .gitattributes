# Git LFS — raw cycler CSVs (up to ~80MB each)
example/fixtures/raw/**/*.csv filter=lfs diff=lfs merge=lfs -text

# Half-cell BOL OCP fixtures
example/fixtures/halfcell/**/*.csv filter=lfs diff=lfs merge=lfs -text
example/fixtures/halfcell/source/**/*.xlsx filter=lfs diff=lfs merge=lfs -text

example/fixtures/doe/**/*.csv filter=lfs diff=lfs merge=lfs -text
